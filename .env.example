# Homebox Companion Environment Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Required: Your OpenAI API key (get one at https://platform.openai.com/api-keys)
HBC_OPENAI_API_KEY=sk-your-api-key-here

# Required: Your Homebox instance URL (without /api/v1 - we add that automatically)
# Examples:
#   - Self-hosted: http://192.168.1.100:7745 or https://homebox.yourdomain.com
#   - Docker on same machine: http://host.docker.internal:7745
#   - Demo server: https://demo.homebox.software
HBC_HOMEBOX_URL=https://your-homebox.example.com

# Optional: OpenAI model for vision (default: gpt-5-mini)
# Options: gpt-5-mini (balanced), gpt-5-nano (faster/cheaper)
HBC_OPENAI_MODEL=gpt-5-mini

# Optional: Logging level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR
HBC_LOG_LEVEL=INFO
